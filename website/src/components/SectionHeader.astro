---
interface Props {
  eyebrow?: string;
  title: string;
  subtitle?: string;
  align?: "left" | "center";
}

const { eyebrow, title, subtitle, align = "left" } = Astro.props;

const alignmentClasses = align === "center" ? "text-center items-center" : "text-left items-start";
---

<div class={`flex flex-col gap-3 mb-12 sm:mb-16 ${alignmentClasses}`}>
  {eyebrow && (
    <div class="inline-flex items-center gap-2">
      <span class="text-xs font-semibold tracking-widest text-[#2997ff] uppercase">
        {eyebrow}
      </span>
    </div>
  )}
  <h2 class="text-3xl sm:text-4xl md:text-5xl font-semibold tracking-[-0.025em] text-[#f5f5f7] leading-tight">
    {title}
  </h2>
  {subtitle && (
    <p class="text-[#86868b] text-base sm:text-lg md:text-xl max-w-2xl leading-relaxed font-normal">
      {subtitle}
    </p>
  )}
</div>
